# 实验记录

这份文档用来记录每一阶段实验到底做了什么、为什么这样做、结果说明了什么。目标是后面写论文和做汇报时，可以用通俗的话讲清楚。

## 当前总思路

最终论文不是只比较 ADMM 收敛快不快，而是要讲一个更完整的问题：真实充电站里车辆是随机来的，离开时间不同，电价也在变。调度算法要一边运行一边决定现在给谁充、充多少，同时不能超过变压器容量。

所以实验要先回答一个最基础的问题：如果我们能生成一个接近真实的充电场景，再比较几种调度方法，是否能看到成本、峰值负荷、准时完成率、公平性之间的差别。

## 第 1 阶段：建立最小可复现实验闭环

本阶段先做四件事：生成随机 EV 充电场景，实现几个基础算法，计算统一指标，输出表格和图。这样后续每加一个算法，都可以放到同一个框架里比较。

当前实现决策如下。

第一，场景采用 48 个半小时时隙，代表一天。每辆车随机到达、随机停留、随机需要充多少电。这样比“所有信息提前给定”的离线问题更接近最终 SCI 主线。

第二，系统同时生成电价、基础负荷和变压器容量。算法做计划时看到的是预测基础负荷，最后检查安全性时用带噪声的实际基础负荷。这样后面可以自然引出 risk buffer 的意义。

第三，先实现四个方法：容量约束下的 uncontrolled charging、简单 deadline/price greedy、offline centralized LP 参考解、online Lyapunov-ADMM。dual decomposition 和 offline ADMM 放到下一阶段，因为它们需要更仔细地对齐数学公式。

第四，统一指标先固定为总成本、峰值负荷、峰均比、未按时完成比例、平均剩余需求、公平性、运行时间、容量违规率。这些指标正好对应论文要讲的 cost、peak、deadline、fairness、scalability。

### 第 1 次运行结果

我先用 50 辆车、48 个时隙、随机种子 7 跑了一次小规模实验。这个场景不算特别拥挤，主要用来检查实验框架是否跑得通。

结果保存在 `outputs/base_experiment/metrics_summary.csv`，主要数字如下。

| 方法 | 总成本 | 总负荷峰值 kW | 未按时完成比例 | 平均剩余电量 kWh | 公平性 | 运行时间 s |
|---|---:|---:|---:|---:|---:|---:|
| uncontrolled_capped | 754.97 | 195.66 | 0.00 | 0.0000 | 1.0000 | 0.0008 |
| greedy_deadline_price | 720.58 | 177.18 | 0.00 | 0.0000 | 1.0000 | 0.0021 |
| offline_centralized_lp | 664.50 | 191.96 | 0.00 | 0.0000 | 1.0000 | 0.0214 |
| online_lyapunov_admm | 751.53 | 164.20 | 0.04 | 0.0120 | 1.0000 | 0.0179 |

这个结果可以这样理解。offline centralized LP 最便宜，因为它提前知道所有车辆信息，可以把充电尽量安排到低电价时段，这是一个“理想参考线”。greedy 方法比 uncontrolled 便宜，也能降峰，说明简单规则已经有用。online Lyapunov-ADMM 的特点是总负荷峰值最低，但为了压峰和在线运行，有 4% 车辆留下了很少量未完成电量。

这里要注意一个问题：当前 50 辆车场景偏容易，所以大多数方法都能完成充电，公平性也都接近 1。它适合作为“框架验证”，但还不足以支撑论文主结果。后面需要增加更紧张的容量场景，才能明显看出 deadline violation、backlog 和 fairness 的差别。

### Lyapunov 参数 V 的小测试

我又单独测试了 `V` 参数。通俗说，`V` 越大，算法越重视电费和削峰；`V` 越小，算法越重视赶紧把车充满。

结果保存在 `outputs/v_sweep/lyapunov_v_sweep.csv`。

| V | 总成本 | 总负荷峰值 kW | 未按时完成比例 | 平均剩余电量 kWh |
|---:|---:|---:|---:|---:|
| 0.5 | 755.24 | 176.54 | 0.00 | 0.0000 |
| 1.0 | 754.12 | 169.80 | 0.02 | 0.0021 |
| 2.0 | 752.43 | 165.78 | 0.04 | 0.0065 |
| 3.0 | 751.53 | 164.20 | 0.04 | 0.0120 |
| 5.0 | 750.61 | 162.89 | 0.04 | 0.0144 |
| 8.0 | 749.99 | 162.13 | 0.04 | 0.0149 |

这个结果已经能讲出论文里的一个核心现象：`V` 是一个调节旋钮。把 `V` 调大，峰值负荷会下降，成本也略降；但算法会更愿意等待低价或低负荷时段，所以 deadline 风险会上升。这就是最终论文中要强调的 cost-delay trade-off。

### 下一步改进

下一步不急着写论文，而是改实验场景。当前场景太轻松，很多指标区分不明显。我要加入一个容量压力参数，让同样的算法在“宽松、中等、紧张”三种场景下比较。这样演讲时就能说清楚：当充电站资源不紧张时，简单算法也能工作；当容量紧张时，在线调度和风险控制才真正重要。

## 第 2 阶段：让场景变紧张，并修正 Lyapunov-ADMM 的 deadline 问题

我加入了 `capacity_factor`。它可以理解为“充电站给 EV 预留多少余量”。数值越大，资源越宽松；数值越小，资源越紧张。

第一次压力测试发现了一个问题：在紧张场景下，原始 online Lyapunov-ADMM 的峰值确实最低，但它太保守，容易为了压峰而拖延部分车辆，导致 deadline violation 偏高。这个现象本身符合 trade-off，但作为主算法还不够好。

所以我做了一个改进：给 Lyapunov-ADMM 加入 deadline safety floor。通俗解释是，如果某辆车现在不至少充一点，后面即使满功率也来不及，那当前时隙就必须给它一个最低充电功率。这不是为了作弊，而是把 deadline 这个硬约束更明确地放进在线决策里。

改进后，在 50 辆车、`V=1.0` 的设置下，容量压力测试结果如下。

| capacity_factor | 方法 | 总成本 | 总负荷峰值 kW | 显著欠充率 | 平均剩余电量 kWh | 容量违规率 |
|---:|---|---:|---:|---:|---:|---:|
| 0.40 | uncontrolled_capped | 754.97 | 195.66 | 0.00 | 0.0000 | 0.00 |
| 0.40 | greedy_deadline_price | 720.58 | 177.18 | 0.00 | 0.0000 | 0.00 |
| 0.40 | offline_centralized_lp | 660.38 | 223.10 | 0.00 | 0.0000 | 0.00 |
| 0.40 | online_lyapunov_admm | 755.02 | 191.36 | 0.00 | 0.0000 | 0.00 |
| 0.32 | uncontrolled_capped | 754.97 | 195.66 | 0.00 | 0.0000 | 0.00 |
| 0.32 | greedy_deadline_price | 720.58 | 177.18 | 0.00 | 0.0000 | 0.00 |
| 0.32 | offline_centralized_lp | 664.50 | 191.96 | 0.00 | 0.0000 | 0.00 |
| 0.32 | online_lyapunov_admm | 754.13 | 169.80 | 0.00 | 0.0000 | 0.00 |
| 0.26 | uncontrolled_capped | 754.87 | 175.32 | 0.02 | 0.0050 | 0.02 |
| 0.26 | greedy_deadline_price | 719.98 | 171.29 | 0.00 | 0.0000 | 0.00 |
| 0.26 | offline_centralized_lp | 678.69 | 171.29 | 0.00 | 0.0000 | 0.00 |
| 0.26 | online_lyapunov_admm | 735.26 | 153.99 | 0.00 | 0.0000 | 0.00 |
| 0.22 | uncontrolled_capped | 746.13 | 159.44 | 0.02 | 0.0199 | 0.02 |
| 0.22 | greedy_deadline_price | 709.60 | 155.41 | 0.10 | 0.0791 | 0.00 |
| 0.22 | offline_centralized_lp | 694.82 | 159.44 | 0.00 | 0.0000 | 0.02 |
| 0.22 | online_lyapunov_admm | 717.32 | 148.02 | 0.00 | 0.0000 | 0.00 |

这组结果现在更像论文结果了。offline centralized LP 仍然是成本最低的理想参考线，但它会在最紧张场景里出现实际容量违规，因为它按预测负荷计划，实际基础负荷有噪声。online Lyapunov-ADMM 成本不是最低，但在中等和紧张容量下，它能把峰值压得最低，同时不欠充、不违规。这个结果正好支撑“在线风险感知调度”的主线。

目前最值得保留的讲法是：offline optimum 负责说明理论最低成本，greedy 说明简单规则的效果，online Lyapunov-ADMM 说明在动态和有风险的场景中，牺牲少量成本可以换来更低峰值和更稳的容量安全。

## 第 3 阶段：补上 risk-aware 实验

最终 SCI 主线里有 risk-aware，所以不能只做普通容量约束。这里的 risk buffer 用 `kappa` 表示。通俗解释是：基础负荷有预测误差，如果一点余量都不留，实际负荷比预测高时就可能超过变压器容量；`kappa` 越大，留的安全余量越多。

我先用 `V=1.0` 测了一次，发现所有 `kappa` 都没有容量违规。这说明该设置下算法本来就偏稳，不能体现 risk buffer 的价值。因此我改用更激进的 `V=0.2` 来测试。这个决定的含义是：先让算法更愿意赶 deadline、更容易贴近容量边界，然后观察安全余量是否能降低实际违规。

结果保存在 `outputs/risk_sweep_v02/risk_buffer_sweep.csv`。

| kappa | 总成本 | 总负荷峰值 kW | 显著欠充率 | 容量违规率 |
|---:|---:|---:|---:|---:|
| 0.0 | 748.41 | 165.06 | 0.00 | 0.0417 |
| 0.5 | 747.51 | 162.25 | 0.00 | 0.0417 |
| 1.0 | 746.22 | 159.44 | 0.00 | 0.0208 |
| 1.5 | 744.01 | 156.63 | 0.00 | 0.0208 |
| 2.0 | 739.32 | 153.82 | 0.00 | 0.0000 |

这组结果可以用于论文里的 risk-aware 分析：增大 `kappa` 后，容量违规率从 4.17% 降到 0%，峰值负荷也下降。这里总成本也下降了，但这个结论要谨慎解释，因为本场景里高负荷时段和高电价时段重叠，保守调度减少了高价时段充电。论文里应该写成“risk buffer improves capacity safety and reduces peak load in this setting”，不要夸大成所有场景下都能降低成本。

到目前为止，已经有三类可用图表：基础方法比较图、容量压力 sweep 图、`V` 参数 sweep 图、risk buffer sweep 图。下一步应该补 scalability，也就是车辆数量从 50 增加到 100、200、500 时，各方法运行时间和指标怎么变化。

## 第 4 阶段：scalability 实验

这一阶段测试车辆数量从 50 增加到 100、200、500 时，各方法是否还能跑得动。这里使用 `capacity_factor=0.32`，也就是容量随车辆规模适度增加，避免大规模场景纯粹因为容量不够而失败。

结果保存在 `outputs/scalability_sweep/scalability_sweep_summary.csv`。

| EV 数量 | 方法 | 总成本 | 总负荷峰值 kW | 显著欠充率 | 运行时间 s | 容量违规率 |
|---:|---|---:|---:|---:|---:|---:|
| 50 | uncontrolled_capped | 754.97 | 195.66 | 0.00 | 0.0005 | 0.00 |
| 50 | greedy_deadline_price | 720.58 | 177.18 | 0.00 | 0.0015 | 0.00 |
| 50 | offline_centralized_lp | 664.50 | 191.96 | 0.00 | 0.0151 | 0.00 |
| 50 | online_lyapunov_admm | 754.13 | 169.80 | 0.00 | 0.0176 | 0.00 |
| 100 | uncontrolled_capped | 1519.26 | 266.79 | 0.00 | 0.0008 | 0.00 |
| 100 | greedy_deadline_price | 1455.10 | 248.35 | 0.00 | 0.0025 | 0.00 |
| 100 | offline_centralized_lp | 1343.82 | 309.91 | 0.00 | 0.0207 | 0.00 |
| 100 | online_lyapunov_admm | 1519.27 | 257.24 | 0.00 | 0.0107 | 0.00 |
| 200 | uncontrolled_capped | 2911.26 | 437.18 | 0.00 | 0.0006 | 0.00 |
| 200 | greedy_deadline_price | 2800.37 | 489.15 | 0.00 | 0.0029 | 0.00 |
| 200 | offline_centralized_lp | 2584.00 | 566.59 | 0.00 | 0.0399 | 0.00 |
| 200 | online_lyapunov_admm | 2911.25 | 437.17 | 0.00 | 0.0075 | 0.00 |
| 500 | uncontrolled_capped | 7863.94 | 1146.97 | 0.00 | 0.0010 | 0.00 |
| 500 | greedy_deadline_price | 7604.34 | 1114.44 | 0.00 | 0.0067 | 0.00 |
| 500 | offline_centralized_lp | 7018.31 | 1318.49 | 0.00 | 0.1060 | 0.02 |
| 500 | online_lyapunov_admm | 7842.31 | 1063.06 | 0.00 | 0.0167 | 0.00 |

这个结果要这样讲：offline centralized LP 成本最低，因为它知道完整未来信息，并且直接针对电价最小化；但它在大规模时会把负荷集中到便宜时段，所以峰值更高，500 辆车时还出现了实际容量违规。online Lyapunov-ADMM 的成本较高，但峰值更低，容量更稳，而且运行时间没有随着车辆数明显恶化。

这里还有一个写论文时要注意的点：本地机器上的 LP 求解器很强，所以不能简单说 centralized “算不动”。更严谨的说法是 centralized 需要完整未来信息和全局数据收集，而 online Lyapunov-ADMM 可以按时隙运行，更适合随机到达和分布式部署。

到这里，第一轮实验闭环已经形成：方法比较、容量压力、`V` trade-off、risk buffer、scalability 都有了初始结果。下一步不是写论文，而是补两个课程相关 baseline：dual decomposition 和 offline ADMM。它们能把第一版论文结构里的内容接回来，也能满足课程截图里“compare existing works”的要求。

## 第 5 阶段：补齐 dual decomposition 和 offline ADMM

我新增了两个 baseline。dual decomposition 表示传统“聚合器发价格信号、车辆按价格自己选充电时段”的方法；offline ADMM 表示第一版论文里的离线分布式优化方法，它知道全天车辆信息，然后用一致性约束协调。

这一步很重要，因为最终论文不能只说“我的方法比 greedy 好”。它还要和课程相关的 decomposition/ADMM 方法比较，才符合截图里 Existing Works 和 Numerical Results 的要求。

50 辆车、默认容量场景下，新增 baseline 后的结果如下。

| 方法 | 总成本 | 总负荷峰值 kW | 显著欠充率 | 运行时间 s | 容量违规率 |
|---|---:|---:|---:|---:|---:|
| uncontrolled_capped | 754.97 | 195.66 | 0.00 | 0.0007 | 0.00 |
| greedy_deadline_price | 720.58 | 177.18 | 0.00 | 0.0022 | 0.00 |
| offline_centralized_lp | 664.50 | 191.96 | 0.00 | 0.0253 | 0.00 |
| dual_decomposition | 672.66 | 195.13 | 0.00 | 0.0659 | 0.00 |
| offline_admm | 665.97 | 191.96 | 0.00 | 3.3634 | 0.00 |
| online_lyapunov_admm | 754.13 | 169.80 | 0.00 | 0.0207 | 0.00 |

这张表可以这样讲：offline centralized 是成本最低的理想解；dual decomposition 和 offline ADMM 都能接近它，说明第一版离线优化思路是成立的；但 online Lyapunov-ADMM 的目标不只是最低成本，它牺牲一些成本，换来了更低峰值和在线运行能力。

完整 capacity sweep 已更新到 `outputs/capacity_sweep_with_baselines/`。紧张场景 `capacity_factor=0.22` 下，有一个很关键的对比：dual decomposition 有 12% 显著欠充，offline centralized 和 offline ADMM 接近低成本但可能出现容量风险，而 online Lyapunov-ADMM 保持 0 欠充、0 容量违规，并且峰值最低。

完整 scalability sweep 已更新到 `outputs/scalability_sweep_with_baselines/`。500 辆车时的运行时间大致是：offline centralized LP 0.17 秒，dual decomposition 0.84 秒，offline ADMM 33.30 秒，online Lyapunov-ADMM 0.015 秒。这里要强调一个公平解释：online 方法每个时隙只解当前问题，不解全天大问题，所以速度快；这不是简单“算法碾压”，而是问题设定从离线变成在线后的部署优势。

到目前为止，实验主线已经比较完整。后面还需要做的是检查图表是否够论文用，必要时再补一张“方法定位图”或“算法流程图”。正文写作仍然应该等实验结果稳定后再开始。

## 当前推荐使用的结果文件

项目里有一些早期试跑输出，后面写论文不要混用。推荐使用下面这几套，因为它们包含最新 baseline 和最新 Lyapunov-ADMM 改进。

基础方法比较使用 `outputs/base_experiment_with_baselines/metrics_summary.csv`、`method_comparison.png` 和 `load_profiles.png`。

容量压力实验使用 `outputs/capacity_sweep_with_baselines/capacity_sweep_summary.csv` 和 `capacity_sweep_summary.png`。如果需要展示最紧张场景下的负荷曲线，用 `outputs/capacity_sweep_with_baselines/factor_0.22/load_profiles.png`。

`V` 参数 trade-off 使用 `outputs/tight_v_sweep/lyapunov_v_sweep.csv` 和 `lyapunov_v_sweep.png`。这组只看 online Lyapunov-ADMM，场景是紧张容量 `capacity_factor=0.22`。

risk buffer 实验使用 `outputs/risk_sweep_v02/risk_buffer_sweep.csv` 和 `risk_buffer_sweep.png`。这里使用 `V=0.2`，因为这个设置更容易观察风险缓冲对容量违规的影响。

scalability 实验使用 `outputs/scalability_sweep_with_baselines/scalability_sweep_summary.csv` 和 `scalability_sweep_summary.png`。

复现实验的核心命令如下。

```powershell
python experiments/ev_charging_experiments.py --mode base --n-ev 50 --seed 7 --kappa 1.0 --V 1.0 --capacity-factor 0.32 --out-dir outputs/base_experiment_with_baselines
python experiments/ev_charging_experiments.py --mode capacity-sweep --n-ev 50 --seed 7 --kappa 1.0 --V 1.0 --out-dir outputs/capacity_sweep_with_baselines
python experiments/ev_charging_experiments.py --mode v-sweep --n-ev 50 --seed 7 --kappa 1.0 --capacity-factor 0.22 --out-dir outputs/tight_v_sweep
python experiments/ev_charging_experiments.py --mode risk-sweep --n-ev 50 --seed 7 --V 0.2 --capacity-factor 0.22 --out-dir outputs/risk_sweep_v02
python experiments/ev_charging_experiments.py --mode scalability-sweep --n-values 50,100,200,500 --seed 7 --kappa 1.0 --V 1.0 --capacity-factor 0.32 --out-dir outputs/scalability_sweep_with_baselines
```

下一步建议做一次“结果合理性检查”：确认每张图讲的故事是否和论文主线一致，删掉不必要的图，把最后报告控制在 8 页以内。现在还不建议正式写正文，但可以开始整理 Numerical Results 的小标题和每张图要表达的一句话。

## 第 6 阶段：图表合理性检查和汇报材料

我检查了五张推荐图。发现早期版本里有两个问题：容量压力图只看 cost 容易误导，因为有些方法成本低是因为没把电充完；scalability 图里的 deadline violation 全是 0，信息量不够。

所以我改了两张图。容量压力图现在展示 total cost、peak total load、unserved energy ratio、capacity violation rate。这样能同时看出“便宜是不是因为没服务完”。scalability 图现在展示 runtime、peak load、total cost、capacity violation rate，不再放空白的 deadline 子图。

我还新增了 `实验结论与汇报提纲.md`，把每类实验浓缩成演讲时能讲的一句话。这个文件后续写论文的 Numerical Results 也可以直接参考。

## 第 7 阶段：英文论文初稿

我已经开始把实验结果转成英文论文。当前新增了 `paper/main.tex` 和 `paper/references.bib`。LaTeX 使用截图要求建议的 IEEEtran 单栏风格，并引用当前实验输出图。

这一步的决定是：先交付 LaTeX 源文件，不在当前环境强行生成 PDF。原因是当前没有检测到 `pdflatex` 和 `bibtex`。代码实验部分已经通过 Python 语法检查，但 PDF 编译需要之后在有 LaTeX 的环境里完成。

我还新增了 `论文写作计划.md`，记录每章要写什么、哪些图进入论文、哪些地方还要检查。这样后面不会在写作时偏离主线。

## 第 8 阶段：根据后续建议增强实验可信度

我参考了 `后续任务及建议.md`。我的判断是：建议方向是对的，尤其是多随机种子统计、ablation、risk correlation、offline ADMM 调参这几项，直接影响论文可信度。所以这一步不是继续堆正文，而是先补实验。

本阶段做了几类改动。

第一，新增了更有区分度的指标。原来的 Jain fairness 大多数时候都接近 1，不太能区分方法。现在增加了 worst-user satisfaction、95th percentile remaining demand、deadline-weighted satisfaction。这些指标更能看出“最差用户”和“紧急用户”有没有被照顾。

第二，新增了 price-load correlation 场景。原来 risk buffer 实验中 `kappa` 变大时成本下降，容易被误解。现在支持 aligned 和 inverted 两种场景：aligned 表示高电价和高基础负荷重叠；inverted 表示高负荷时电价反而较低。这样可以更严谨地说明：risk buffer 的主要作用是降低容量违规，不是保证降低成本。

第三，新增了 ablation。现在可以比较 full online L-ADMM、去掉 deadline floor、去掉 risk buffer、去掉 Lyapunov queue、greedy baseline。这个实验用来回答“每个模块到底有没有贡献”。

第四，新增了 offline ADMM 的 `rho` sweep。这样可以检查 offline ADMM baseline 是否调参公平，而不是随便设一个 `rho=1`。

第五，新增了 multi-seed baseline。当前先跑了 10 个随机种子，输出在 `outputs/multiseed_base_10/`。这比只看 `seed=7` 更可靠。

### 10 个随机种子的 baseline 统计

结果文件：`outputs/multiseed_base_10/multiseed_base_summary.csv`。

关键结果如下。

| 方法 | 平均成本 | 成本标准差 | 平均峰值 kW | 峰值标准差 | 未服务能量比例 | 容量违规率 | 平均运行时间 s |
|---|---:|---:|---:|---:|---:|---:|---:|
| offline_centralized_lp | 772.32 | 46.53 | 188.05 | 11.83 | 0.000000 | 0.0104 | 0.0173 |
| offline_admm | 774.11 | 46.51 | 187.76 | 11.73 | 0.000000 | 0.0104 | 4.2583 |
| dual_decomposition | 775.35 | 47.02 | 186.63 | 11.18 | 0.000090 | 0.0042 | 0.0978 |
| greedy_deadline_price | 828.34 | 44.32 | 185.35 | 9.98 | 0.000432 | 0.0021 | 0.0023 |
| uncontrolled_capped | 853.76 | 43.90 | 177.45 | 15.20 | 0.000011 | 0.0021 | 0.0008 |
| online_lyapunov_admm | 849.99 | 42.44 | 164.45 | 7.70 | 0.000000 | 0.0000 | 0.0220 |

这组结果非常适合论文使用。它说明趋势不是单随机种子的偶然现象：offline centralized 和 offline ADMM 成本最低；online Lyapunov-ADMM 成本较高，但平均峰值最低，而且容量违规率为 0。演讲时可以说：在线方法不是为了赢最低电费，而是为了更稳地控制峰值和风险。

### V 参数重新选择

由于 price-load 模式更新后，原来的 `V=1.0` 在部分紧张场景下略保守，我重新跑了更密的 V sweep。结果文件：`outputs/tight_v_sweep_refined/lyapunov_v_sweep.csv`。

结论是：`V=0.8~1.2` 是比较合理的工作区间。`V>=1.5` 时 deadline violation 明显上升。后续主实验建议用 `V=0.8` 或 `V=1.0`，其中 `V=0.8` 更适合 ablation，因为它对 deadline 更稳。

### Ablation 结果

结果文件：`outputs/ablation_3_v08/ablation_summary.csv`。

| 变体 | 平均成本 | 平均峰值 kW | 未服务能量比例 | 容量违规率 |
|---|---:|---:|---:|---:|
| full_online_ladmm | 780.56 | 152.74 | 0.0115 | 0.0069 |
| greedy_deadline_price | 766.97 | 157.84 | 0.0267 | 0.0139 |
| no_deadline_floor | 772.45 | 151.11 | 0.0234 | 0.0069 |
| no_lyapunov_queue | 709.85 | 152.78 | 0.0679 | 0.0069 |
| no_risk_buffer | 790.32 | 155.27 | 0.0035 | 0.0069 |

这组结果要谨慎讲。full 方法不是每个指标都最优，但它更平衡。去掉 Lyapunov queue 后成本最低，但未服务能量最高，说明它是通过牺牲用户服务来省成本。去掉 deadline floor 后未服务能量变大，说明 deadline floor 有必要。risk buffer 会改变容量可用空间，因此它和服务完成率之间存在权衡。

### Risk correlation 结果

结果文件：`outputs/risk_correlation/risk_correlation_sweep.csv`。

aligned 场景下，`kappa` 从 0 增加到 2，容量违规率从 4.17% 降到 0%，成本也下降。这是因为高负荷和高电价重叠，保守调度减少了高价高峰充电。

inverted 场景下，`kappa` 从 0 增加到 2，容量违规率从 8.33% 降到 0%，但成本从 563.11 增加到 574.85。这个结果很重要，它证明 risk buffer 的一般作用是降低容量风险，而不是保证降低成本。

### Offline ADMM rho sweep

结果文件：`outputs/offline_admm_rho_sweep/offline_admm_rho_sweep.csv`。

`rho=0.1` 的 residual 最均衡，runtime 约 3.33 秒；`rho=1` 成本接近 centralized，但 dual residual 更高；`rho=10` 能量满足很干净，但成本明显变高。后续如果论文强调 offline ADMM baseline，建议报告 `rho=0.1` 或说明 `rho` sweep 后选择了稳定参数。

### 当前更合理的论文结论

不要写“online Lyapunov-ADMM 全面优于其他方法”。更准确的结论是：

offline methods win in cost because they use complete future information; online Lyapunov-ADMM provides a deployment-oriented trade-off with lower peak load, lower capacity risk, and online adaptability.

## 第 9 阶段：对照后续建议补齐剩余缺口

我重新对照了 `后续任务及建议.md`，发现大部分高优先级实验已经完成，但还有三个能在当前环境继续补强的点：第一，baseline 统计最好从 10 个随机种子提高到 30 个随机种子；第二，场景不能只保留默认 synthetic，最好增加一个更接近工作场所充电站的敏感性场景；第三，论文里不能只比运行时间，还要说明每种方法是否需要未来信息、是否在线、是否分布式。

所以本阶段做了三件事。

第一，把 baseline comparison 扩展到 30 个随机种子。新结果在 `outputs/multiseed_base_30/`。30-seed 后，offline centralized LP 平均成本最低，为 752.98；online Lyapunov-ADMM 平均成本为 825.51，仍然更高。但 online Lyapunov-ADMM 的平均峰值最低，为 163.28 kW，而 offline centralized LP 是 186.34 kW；online 的容量违约率仍然是 0。这个结果说明主结论不是 10 个种子的偶然现象。

第二，新增 `--scenario-profile workplace`。这个场景模拟工作场所充电站：车辆主要早晨到达，停留时间接近一个工作日，需求分布更集中。10 个随机种子的结果在 `outputs/workplace_multiseed_10/`。结果仍然是 offline 方法成本更低，online Lyapunov-ADMM 峰值更低且容量违约为 0。这里要谨慎：workplace profile 只是半真实敏感性检查，不等于已经完成真实公开数据验证。

第三，论文里加入了方法属性表。这个表说明 uncontrolled、greedy、offline centralized、dual decomposition、offline ADMM、online Lyapunov-ADMM 分别是否需要未来信息、是否需要全局数据、是否在线、是否分布式。这样讲 scalability 时更严谨：不是说 centralized LP 算不动，而是说它依赖完整未来信息；online Lyapunov-ADMM 的优势是可以实时滚动运行。

当前还没完成的主要是外部依赖项：真实公开数据校准、PDF 编译和 8 页检查、40+ 文献逐条核对。它们需要数据文件、联网检索或 LaTeX 环境，不能只靠当前本地代码完全闭环。

## 第 10 阶段：补大规模 fast scalability

建议文件里提出 scalability 最好继续扩展到 1000、2000 EV。直接跑完整六方法会比较重，因为 offline ADMM 在 500 EV 时已经约 33 秒。为了先回答“大规模下 online 方法还能不能快速运行”这个问题，我新增了 `scalability-fast` 模式。

`scalability-fast` 只跑四个方法：uncontrolled capped、greedy deadline-price、offline centralized LP、online Lyapunov-ADMM。它暂时跳过 offline ADMM 和 dual decomposition。这个决定的原因是：这一步关注大规模趋势，不是重新评估所有 offline decomposition baseline。

已运行命令：

```powershell
python experiments/ev_charging_experiments.py --mode scalability-fast --n-values 1000,2000 --seed 7 --kappa 1.0 --V 0.8 --capacity-factor 0.32 --method-set fast --out-dir outputs/scalability_fast_large
```

关键结果如下。

| 车辆数 | 方法 | 成本 | 峰值 kW | runtime s | 容量违约率 |
|---:|---|---:|---:|---:|---:|
| 1000 | offline_centralized_lp | 15407.09 | 2556.03 | 0.2400 | 0.0000 |
| 1000 | online_lyapunov_admm | 16963.93 | 1934.56 | 0.0103 | 0.0000 |
| 2000 | offline_centralized_lp | 30367.90 | 5030.35 | 0.4118 | 0.0208 |
| 2000 | online_lyapunov_admm | 33634.83 | 3670.17 | 0.0110 | 0.0000 |

这个结果要这样讲：centralized LP 在当前合成 LP 场景下仍然很快，所以不能说它算不动；但它需要完整未来信息，并且在 2000 EV 下出现实际容量违约。online Lyapunov-ADMM 仍然保持很低 runtime 和 0 容量违约，说明它的大规模在线滚动部署性质更好。

## 第 11 阶段：补 centralized per-slot 消融

建议文件里还提到一个重要消融：不使用 ADMM，直接集中式求每个时隙的在线问题。这个对照的意义是区分“目标函数本身的效果”和“ADMM 分布式求解架构的效果”。

我新增了 `online_centralized_slot`。它和 online Lyapunov-ADMM 使用相同的当前状态、deadline floor、risk buffer 和 Lyapunov 权重，但每个时隙用集中式 SLSQP 求解，而不是 ADMM 分解。

重跑 `outputs/ablation_3_v08/` 后，关键结果如下。

| 变体 | 成本 | 峰值 kW | 未服务能量比例 | 最差用户满意度 | runtime s |
|---|---:|---:|---:|---:|---:|
| full_online_ladmm | 782.66 | 149.92 | 0.0076 | 0.965 | 0.0393 |
| online_centralized_slot | 789.11 | 151.79 | 0.0037 | 0.983 | 0.2893 |
| no_deadline_floor | 775.79 | 149.21 | 0.0181 | 0.867 | 0.0385 |
| no_lyapunov_queue | 712.49 | 150.46 | 0.0620 | 0.817 | 0.0206 |

这个结果要这样讲：centralized per-slot 的服务质量不错，但它是集中式求解，运行时间更高，也没有分布式结构。ADMM 的价值不是让目标函数“变聪明”，而是把同一个在线时隙问题拆成车辆侧和聚合器侧，更适合部署。

运行时 SciPy SLSQP 出现了 “Values in x were outside bounds during a minimize step, clipping to bounds” warning。这是 SLSQP 迭代过程中的裁剪警告，最终结果仍然通过投影回到 box 和 capacity 约束内。该 warning 已记录，后续如果要正式使用 centralized per-slot 结果，可以进一步改成更专门的凸二次规划求解器。

## 第 9 阶段：把稳定实验转成论文证据

本阶段没有再盲目增加新实验，而是把已经稳定的实验结果整理成论文里可直接使用的证据。原因是：实验做到一定程度后，最重要的不是继续跑更多图，而是确认哪些数字能支撑主线，哪些说法不能夸大。

我更新了 `paper/main.tex`。新增内容包括：实验设置表、10 个随机种子的 baseline 统计表、ablation 消融表，以及 Basic Properties 理论说明小节。

实验设置表的作用是说明实验覆盖面。当前实验覆盖了 24 小时、48 个半小时时隙、50 辆车默认场景、多随机种子、容量压力、`V` 参数、`kappa` 风险缓冲、50 到 500 辆车扩展性。这能回答“实验是不是只挑了一个有利场景”的问题。

10-seed baseline 表的核心结果是：offline centralized LP 平均成本 772.32，offline ADMM 平均成本 774.11，online Lyapunov-ADMM 平均成本 849.99；但 online Lyapunov-ADMM 的平均峰值只有 164.45 kW，是所有方法里最低的，并且容量违规率是 0。这个结果要写成“用更高成本换取在线削峰和安全性”，不能写成“全面优于所有方法”。

ablation 表的核心结果是：去掉 Lyapunov queue 后成本最低，但未服务能量比例最高；去掉 deadline floor 后欠充增加；risk buffer 改变容量裕度，所以它主要用于安全性，不是保证成本最低。这个实验支撑“本文各模块都有作用”。

Basic Properties 小节说明了方法为什么能用 ADMM：每个时隙内，目标函数由线性项和非负二次项组成，约束是线性的，因此是凸问题；再把车辆本地变量和聚合器全局约束拆开，就形成 ADMM 适合处理的结构。

到这里，实验已经从“能跑出图”推进到“能支撑论文论证”。下一步应该检查 LaTeX 能否编译、页数是否满足 8 页限制，并根据页数决定保留哪些图表。

## 第 12 阶段：真实公开数据验证、参考文献和 PDF 检查

本阶段补了之前最关键的三个外部缺口：真实公开数据、40+ 参考文献、PDF 编译和 8 页检查。

真实数据方面，我没有选择 ACN-Data 作为主落点，因为 ACN-Data API 通常需要 token，不适合写成“任何人直接运行就能复现”。这次选的是 4TU/ElaadNL 的办公停车场公开数据集，DOI 是 `10.4121/80ef3824-3f5d-4e45-8794-3b8791efbd13.v2`。它包含 Utrecht 一个大型办公停车场的真实充电会话，字段包括 `start_datetime`、`end_datetime` 和 `total_energy`。

我新增了两个真实数据模式：`real-data-base` 和 `real-data-multiday`。它们不会替换原来的 synthetic/workplace 实验，而是额外读取公开 CSV，把真实车辆的到达时间、离开时间和充电电量转成调度实例。注意，这不是完整复现原停车场当时的控制器，因为电价和基础负荷不在这个 CSV 里，仍然由本项目的评估框架生成。因此论文里应该写成“public-data check”或“external-validity check”，不要写成“field deployment”。

已运行 5 天真实数据检查，结果在 `outputs/real_elaadnl_multiday_5/`。程序自动选择会话最多的 5 天，每天最多抽 50 个真实会话。关键均值如下。

| 方法 | 平均成本 | 平均峰值 kW | deadline violation | 容量违约率 |
|---|---:|---:|---:|---:|
| offline_centralized_lp | 1002.02 | 208.26 | 0.000 | 0.038 |
| offline_admm | 1003.55 | 208.28 | 0.000 | 0.033 |
| dual_decomposition | 999.98 | 207.92 | 0.048 | 0.025 |
| greedy_deadline_price | 1040.37 | 208.11 | 0.144 | 0.033 |
| uncontrolled_capped | 1035.13 | 211.17 | 0.228 | 0.062 |
| online_lyapunov_admm | 1048.30 | 198.49 | 0.108 | 0.012 |

这个结果要谨慎讲。它支持 online Lyapunov-ADMM 降低峰值和容量风险：峰值从 offline centralized 的 208.26 kW 降到 198.49 kW，容量违约率也从 3.8% 降到 1.2%。但它也暴露出真实办公会话更紧张，`V=0.8` 下 online 方法有 10.8% 的 deadline violation。补跑 `V=0.2` 后 deadline violation 可以降到 0，但峰值和容量风险会变差。所以真实数据给出的结论不是“我的方法一定最好”，而是“真实场景下必须联合调 `V` 和 `kappa`，成本、deadline、容量安全三者存在权衡”。

参考文献方面，`paper/references.bib` 已经扩展到 45 条，其中 44 条带 DOI，正文 45 条全部引用，没有未引用条目。新增文献覆盖 EV 智能充电、V2G、分布式优化、ADMM、Lyapunov/MaxWeight、鲁棒/机会约束、强化学习对照、ACN-Data 和 ElaadNL/办公楼真实数据。

PDF 方面，本机没有 `pdflatex/bibtex`，所以我下载了本地 Tectonic 单文件 TeX 引擎到 `tools/tectonic/`。最终 `paper/main.tex` 已成功编译成 `paper/build/main.pdf`。第一次带 6 张大图时页数是 15 页，不满足要求；随后把论文改成“8 页优先版本”：保留核心表格，正文描述图表趋势，不嵌入所有大图。最新 PDF 是 8 页，满足单栏 11pt 的页数要求。编译没有 undefined citation，也没有 overfull hbox；只有 Tectonic 在 Windows 下的字体替代 warning。

已跟踪的 `experiments/__pycache__/ev_charging_experiments.cpython-312.pyc` 已用 `git rm --cached` 从版本跟踪中移除，后续由 `.gitignore` 忽略。这样以后运行实验不会再让 `.pyc` 反复显示修改。

## 第 13 阶段：按审稿人视角精修论文和图表

这一阶段我接受了你最新建议里的核心判断：论文不能写成“我的方法全面最好”，而要写成“在线、分布式、风险感知的部署型权衡”。原因很简单：离线 centralized LP 和 offline ADMM 知道完整未来信息，电费更低是正常的；本文方法的价值不在于击败它们的最低成本，而在于实时滚动运行、削峰、降低容量风险，并保留分布式协调结构。

正文做了四类改动。第一，摘要和结论改得更克制，不再把公开数据写成完整真实场站验证，而是明确写成 external-validity check。第二，理论部分从描述性文字改成 Proposition 形式：每个时隙问题是凸问题，ADMM 在标准闭凸可行和可解条件下收敛；Lyapunov 部分只写成理论解释，不假装已经证明新的 `O(1/V)` 和 `O(V)` 界。第三，方法部分加入了一个紧凑的在线算法表，比大段文字更像正式优化论文。第四，正文加入一张简洁的两栏图，展示成本-峰值权衡和 `V` 参数敏感性。

新图由 `experiments/make_paper_figures.py` 生成，输出在 `paper/figures/summary_tradeoffs.png` 和 `paper/figures/summary_tradeoffs.pdf`。我没有继续使用那些颜色很重、子图很多的实验总览图，因为它们更适合汇报，不太像论文图。现在这张图只保留最关键的信息：online Lyapunov-ADMM 不是最低成本，但峰值更低；`V` 增大可以降成本，但过大后 deadline violation 会出现。

PDF 已重新编译为 `paper/build/main.pdf`，仍然是 8 页。我渲染检查了第 1 页、第 5 页和第 8 页：标题和摘要正常，图 1 白底清晰，没有黑底或裁切，参考文献页没有溢出。编译日志没有 undefined citation 和 overfull hbox；只剩 Windows/Tectonic 字体替代 warning，不影响可读性。

## 第 14 阶段：全文英文通读和语气压缩

这一阶段只改论文表达，不改实验、表格和图。主要目标是让英文更像正式论文，减少重复句子和口语化表达。

我重点压缩了三类句子。第一，减少反复出现的 “the proposed method should be interpreted ...” 这类模板句，改成更自然的 “deployment-oriented interpretation” 或 “targets causal scheduling”。第二，把 “easier to interpret and tune” 这类偏口语的表达改成 “interpretability and tunability”。第三，把真实数据相关表述继续收紧，例如把 “real office sessions” 改成 “public office-parking sessions”，避免读者误以为电价和基础负荷也来自真实场站。

修改后重新编译 `paper/build/main.pdf`，结果仍是 8 页。45 条参考文献全部被引用，没有 missing citation，没有 undefined citation，也没有 overfull hbox。重新渲染检查第 1 页、第 5 页和第 8 页后，标题摘要、图表和参考文献都正常。
